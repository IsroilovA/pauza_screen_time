/// Permission feature method names.
///
/// Keep in sync with native handlers.
class PermissionsMethodNames {
  const PermissionsMethodNames._();

  static const String checkPermission = 'checkPermission';
  static const String requestPermission = 'requestPermission';
  static const String openPermissionSettings = 'openPermissionSettings';
}
